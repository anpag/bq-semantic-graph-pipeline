config {
  type: "operations",
  hasOutput: true,
  name: "gemini_pro",
  description: "Vertex AI Gemini 2.5 Pro model for text and multimodal generation."
}

CREATE MODEL IF NOT EXISTS ${self()}
REMOTE WITH CONNECTION `${dataform.projectConfig.vars.connection_id}`
OPTIONS(ENDPOINT = 'gemini-2.5-pro');
